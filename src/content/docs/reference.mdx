---
title: Reference
description: CLI, API, state file format, and configuration reference for Ordo.
---

The reference covers the surfaces operators interact with directly:

- **CLI** — every command exposed by the `ordo` binary, with examples
- **API** — the orchestrator's HTTP management API
- **State file format** — the YAML schema for declarative state modules
- **Configuration** — every option in the orchestrator and agent configuration files, with types, defaults, and examples

:::caution
Reference content is the last section to be written. The CLI, API, state file, and configuration surfaces are still moving while desired state and secret distribution land. Pages will appear here once the surfaces they describe are stable enough to document without immediate rework.
:::
